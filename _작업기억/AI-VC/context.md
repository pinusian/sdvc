# context.md — AI-VC 플랫폼

## 주요 결정
- "AI-VC" = 교과서 제1권에서 정의한 AI 바이브코딩 방법론/스킬의 정식 명칭.
- `02_플랫폼_프로젝트\_스킬인프라\` 를 `AI-VC\` 로 승격·개칭(사용자 지침).
- 코드 백업은 ALCP와 동일하게 git bundle 방식. 두 저장소 모두 origin/main과 동기화 상태라 로컬 우선순위 이슈 없음.
- 교과서·교재·가이드 docx 는 이미 00/03 에 있으므로 AI-VC 폴더에는 중복 복사하지 않고 `_플랫폼_개요.md` 에서 링크로만 연결.
- 스킬 SKILL.md: repo판 vs 로컬 원본판이 마지막 문단만 다름(로컬 경로 안내). references 11종은 동일.

## 참고
- 스킬 원본 경로 안내 문구가 아직 옛 이름 `AI바이브코딩` 으로 되어 있음(현재 폴더명 `AI Vibecoding`). 다음 스킬 수정 시 정정 대상.
