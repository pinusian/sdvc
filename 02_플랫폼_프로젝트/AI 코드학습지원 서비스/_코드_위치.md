# AI 코드학습지원 서비스 (ALCP) — 실제 코드 위치

> 확인일: 2026-09-04

## 원본(작업용, source of truth)

**`C:\Users\USER\AI_Code_Study\`**

- GitHub 원격: `https://github.com/pinusian/ai-code-learning-platform.git`
- 현재 브랜치: `005-reading-activity` (그 외 `master`, `002-user-auth`, `003-project-management`, `004-task-learning-content`)
- 최신 커밋: `f9fdd0d` (2026-08-29) "docs: progress.md 신설 … 005-reading-activity 58/58 완료, 테스트 88 passed"
- ⚠️ **로컬이 원격보다 앞섬**: `005-reading-activity` 브랜치 기준 **원격에 안 올라간 커밋 7개** + spec 파일 수정 미커밋 → **로컬 폴더가 최신 authoritative 사본**.
- 화면: `http://localhost:5173` (webapp, 할일·인증·프로젝트·Task) / `http://localhost:5174` (webapp-reading, 독서활동). 첨부 스크린샷 "내 프로젝트" 화면 = `webapp/src/pages/ProjectsPage.tsx`.

## 구성

| 영역 | 경로 | 스택 |
|---|---|---|
| 백엔드 | `backend/src/` | FastAPI + SQLAlchemy + SQLite(`backend/auth.db`), Anthropic API |
| 메인 프런트 | `webapp/` (:5173) | React 19 + Vite + TS |
| 독서활동 프런트 | `webapp-reading/` (:5174) | React 19 + Vite + TS |
| (구버전) | `frontend/` | 순수 JS — 미사용, 초기 프로토타입 |
| 명세 | `specs/001~005/`, `.specify/` | Spec Kit (SDD) |
| 문서 | `docs/` (progress.md, run-guide.md, task-reports/) | |
| AI 생성물 | `storage/discourse/*.docx` | git 미추적 |
| 실행 | `start-servers.bat`, `restart-backend.bat` | |

규모: git 히스토리 4.9MB, 작업트리 8.7MB (node_modules·.venv 제외), 추적 파일 238개.

## 혼동 주의 — 아래는 옛/다른 버전 (백업 대상 아님)

| 경로 | 정체 |
|---|---|
| `C:\Users\USER\Claude작업용폴더\Claude_CC\code_study-main\` | `code_study-main.zip`(2026-08-22) 압축 해제본. git 커밋 없음 → 초기 상태 스냅샷 |
| `C:\Users\USER\Claude작업용폴더\Claude_CC\AI 코드 학습 서비스\` | Node.js `server/server.js` + 정적 HTML 기반의 **별개 초기 프로토타입**. 현재 FastAPI/React 버전과 무관 |

## 백업 완료 (2026-09-04)

이 폴더에 다음을 저장함:
- `AI_Code_Study.bundle` — git 전체 히스토리(11 refs, 437KB)
- `_uncommitted_changes.patch` — 스냅샷 시점 미커밋 변경분
- `restart-backend.bat` — 502 복구 스크립트
- `_구조도.md` — 아키텍처 + 복원 방법

갱신 방법: 원본에서 `git bundle create "<이폴더>\AI_Code_Study.bundle" --all` 재실행.
