# 상시 접수함 (Backlog)

> 쓰다가 발견한 문제와 개선 요청을 **발견 즉시** 여기에 등록한다.
> 매번 계획을 새로 짜지 않기 위한 장치다 — 모아 두었다가 슬라이스 단위로 처리한다.

## 처리 규칙

| 영향도 | 뜻 | 처리 |
|---|---|---|
| **치명** | 이미 파는 기능이 동작하지 않는다 | 진행 중인 작업을 멈추고 **즉시** 끼워넣는다 |
| **높음** | 쓸 수는 있으나 사용자가 곧 막힌다 | 다음 슬라이스에 넣는다 |
| **보통** | 불편하지만 우회할 수 있다 | 모아서 다듬기 슬라이스로 |
| **낮음** | 있으면 좋다 | 여유 있을 때 |

등록할 때는 **증상 · 재현 방법 · 영향도**를 적는다. 재현 방법이 없으면 고쳤는지 확인할 수 없다(SC-008).
처리되면 `상태`를 바꾸고 관련 작업 ID와 커밋을 남긴다. 지우지 않는다 — 같은 문제가 다시 나올 때의 단서다.

## 접수 목록

| ID | 등록일 | 증상 / 요청 | 재현 | 영향도 | 상태 | 처리 |
|---|---|---|---|---|---|---|
| BL-027 | 2026-09-18 | **로그아웃 기능이 없다.** Story-Doing 화면에 "로그아웃 기능은 아직 제공되지 않습니다. 다른 계정으로 써보시려면 시크릿(비공개) 창을 이용해주세요"라고 적혀 있었다 | 실사용자가 BL-025·BL-026 수정을 확인하던 중 직접 요청("로그아웃 기능이 없어서 넣어줘") | **보통** (있으면 좋다지만 시크릿 창으로 우회 가능했다) | 처리 완료 | **[P11-2]가 로그인·가입은 만들었지만 로그아웃을 빠뜨렸었다** — 서버에 라우트 자체가 없었다. `POST /api/site/[slug]/auth/logout` 신규(DB 안 건드리고 쿠키만 [BL-026]과 같은 path로 지움, 로그인 여부와 무관하게 항상 200). [P11-5] 안내에도 추가하고 "로그인 화면에는 반드시 로그아웃 버튼도 함께 낸다"고 못 박음 — 앞으로 다른 프로젝트에서 같은 누락이 반복되지 않게. 실제 브라우저로 로그인→로그아웃→`/records` 재차단까지 재현 확인. 커밋 `f361ca6` |
| BL-026 | 2026-09-18 | **[BL-025] 수정 직후 바로 발견.** 로그인은 되는데(가입·로그인 응답은 정상), 그 뒤 API 키 저장을 누르면 "로그인이 필요합니다"만 뜨고 저장이 안 됨 | 실사용자 프로젝트 "Story-Doing_독서활동"에서 로그인 직후 API 키 저장 시도 시 재현. 서버 로그로 `POST .../settings/api-key`가 401 | **치명** (로그인은 되는 것처럼 보이지만 그 뒤 모든 방문자 API가 실제로는 인증되지 않는다 — 기록 작성·조회·AI 요약 전부 같은 이유로 막혀 있었을 것) | 처리 완료 | **원인**: 세션 쿠키를 `path=/site/{slug}`(그 프로젝트의 **페이지** 주소)로 심었는데, 이 쿠키를 실제로 읽는 곳은 전부 `/api/site/{slug}/...`(**API** 주소)다. 브라우저는 쿠키의 Path가 요청 URL의 접두사일 때만 쿠키를 실어 보내는데 둘이 접두사 관계가 아니라, 로그인 직후 응답 본문으로 화면만 바뀌었을 뿐 그 뒤 어떤 요청도 실제로는 인증되지 않고 있었다. `path`를 `/api/site/{slug}`로 바로잡음. **[P11-7]의 "전 구간 실제 검증"이 왜 이걸 못 잡았는지도 기록**: 그 검증은 `Set-Cookie` 값을 수동으로 복사해 매 요청에 직접 붙이는 방식이라 브라우저의 Path 매칭을 거치지 않았다 — 실제 브라우저 없이는 이 버그가 절대 드러나지 않는 종류였다. 이번엔 실제 브라우저(로컬 dev, 같은 프로덕션 DB)로 가입→API 키 저장 성공까지 재현 확인. 커밋 `3d2203a` |
| BL-025 | 2026-09-18 | **방문자 계정(로그인·회원가입) 기능을 넣은 산출물에서 회원가입이 전부 실패한다.** 브라우저 콘솔에 `Access to fetch … from origin 'null' has been blocked by CORS policy`, `localStorage … sandboxed and lacks the 'allow-same-origin' flag` | 실사용자 프로젝트 "Story-Doing_독서활동"([P11-5] 안내로 방문자 계정 기능을 새로 넣은 직후) — 실제 배포 주소(`https://sdvc-app.vercel.app/site/story-doing`)에서 재현. 처음엔 "로컬 파일로 열어서 그렇다"고 잘못 짚었으나, 실제 배포 주소에서도 그대로 재현되어 정정 | **치명** (Phase 11 전체가 링크공개·전체공개 프로젝트에서는 아예 못 쓴다 — private만 된다) | 처리 완료 | **원인은 [P5-1]의 의도된 보안 설정**: 공개·링크공개 산출물에 `Content-Security-Policy: sandbox allow-scripts allow-forms allow-popups`를 붙이면서 `allow-same-origin`을 일부러 뺐다(공개 산출물의 스크립트가 다른 개발자의 로그인 세션을 같은 출처 요청으로 도용하지 못하게). 그런데 이게 Phase 11이 필요로 하는 fetch·쿠키·localStorage까지 전부 막았다. `allow-same-origin`을 추가하면 그 위험이 되살아나지만, **비공개 프로젝트는 이미 같은 위험을 감수하고 있다**(같은 이유로 sandbox 자체를 안 검) — 노출 범위만 넓어질 뿐 새 위험은 아니라고 보고, 사용자에게 트레이드오프를 설명한 뒤 승인받아 `allow-same-origin` 추가. 로컬 dev 서버(같은 프로덕션 DB)에서 실제 회원가입 성공까지 재현 확인(테스트 계정은 확인 후 삭제). 커밋 `cc51338` |
| BL-023 | 2026-09-14 | **구현 단계의 AI가 실행하지도 않은 테스트 결과를 보고한다.** "`$ node --test tests/records.test.js` … tests 7 pass 7" 같은 터미널 출력을 답변에 적는다 | 실사용자 대화(`f85ca87b…`, 슬라이스 1·2). 이 서비스의 서버에는 **코드를 실행하는 수단이 없다** — 모델은 파일을 `file:` 블록으로 낼 뿐이다. 그런데 구현 지시문은 "RED→GREEN, 완료 보고에 실행한 명령과 출력을 제시"를 요구한다 → 모델이 출력을 **지어낸다** | **높음** (헌장의 '증거 기반'과 정면으로 어긋난다. 사용자는 검증된 줄 안다) | 처리 완료 | 구현 지시문에서 "실행한 명령과 그 출력을 제시"를 걷어내고, 검사는 **사용자가 브라우저로 여는 `tests.html`**로 내게 했다(열면 통과/실패가 화면에 표시됨). 모든 블록의 관통 규칙에도 "이 환경에서는 코드를 실행할 수 없다"를 명시. **실제 Claude 호출로 검증**: 할 일 목록 앱 구현 요청 → 지어낸 실행 출력 0건, tests.html(검사 6개)을 내고 "이 화면을 열어 확인해 달라"고 정확히 요청, 산출물 5개 파일 실제 배포까지 확인(확인 후 정리). 커밋 `b129133`→`809ea81` |
| BL-022 | 2026-09-14 | **새 프로젝트가 계획 단계부터 진행이 안 된다.** 사용자 메시지 7건에 답이 0건, "다음 단계로"를 누를 때마다 단계만 넘어가 **계획서·작업분해 없이 구현 단계**에 도착 | DB로 확정: 60초 안에 끝난 헌장·명세·명확화(11~51초)는 저장, 답이 긴 계획 이후는 전부 60초에 강제 종료 — 답변·사용량 모두 미저장. Vercel 문서: Hobby 기본·상한 **300초** | **치명** (계획·작업분해·구현이 모든 사용자에게 막혔다) | 처리 완료 | **원인은 내 수정 [BL-021b]**: `maxDuration=60`으로 한도를 늘린 게 아니라 300→60으로 **줄였다**(전날 85초 정상 동작을 증거로 거꾸로 읽음). ① 300으로 되돌림(`c865f59`) ② 답 없는 승인은 단계를 안 넘김 ③ done을 저장·발행 뒤 맨 끝에 보냄 ④ done 없이 닫히면 화면이 '도중에 끊겼다'고 알림(`3efa15f`). 되돌린 뒤 같은 대화에서 150초·202초짜리 답변이 정상 저장됨을 확인. **대화 복구(단계를 계획으로 되돌리기)는 하지 않았다** — 그 사이 AI가 스스로 누락을 짚고 승인받아 구현이 정상 진행 중이었다. 함께 발견: [BL-021] 테스트 describe가 API 키를 안 넣어 한 건이 헛통과였다 — 바로잡음 |
| BL-021 | 2026-09-13 | **"이어서 수정"에서 요청을 보내니 `요청에 실패했습니다. (상태 500)`.** 사용자가 빵집 홈페이지 프로젝트에 회원가입·운영자화면·문의게시판 추가를 요청한 순간 발생 | **원인 재현 실패, 그러나 조사로 좁힘.** ①제 PWA 변경과 무관(프로덕션은 `b59a40a`) ②**메시지가 저장되지도, Claude가 불리지도 않았다**(09:00 UTC 이후 `messages` 0건·`usage_logs` 0건) → `appendMessage` **이전**에 죽었다 ③500 본문에 JSON `error`가 없었다(ChatView가 기본 문구로 떨어짐) → 우리 라우트의 응답이 아니라 **플랫폼 500**(처리되지 않은 예외/함수 실패) ④같은 프로젝트·대화(파일 16개·메시지 34건·`current_block=done`)를 통째로 복제해 로컬·프로덕션에서 **같은 문장으로 재시도했으나 둘 다 재현 안 됨** | **높음** (유지보수가 이 서비스의 핵심인데 입력한 글까지 사라진다) | 관찰 | 곁가지 3건(BL-021a·b·c)은 **처리 완료**(커밋 `fa73470`). 본체는 **원인 미상** — 로컬·프로덕션 재현 모두 실패했다. 다시 나오면 이제는 맨 500이 아니라 우리 문구가 뜨고 입력도 남으므로 증상을 더 정확히 받을 수 있다 |
| BL-021a | 2026-09-13 | **`/api/chat`에 최상위 try/catch가 없다.** 예상 못 한 예외가 나면 JSON 없는 맨 500이 나가고, 사용자가 **공들여 쓴 긴 요청이 통째로 사라진다** | 코드 확인(`src/app/api/chat/route.ts`) — 명시적 500은 API 키 미설정 하나뿐 | **높음** | 처리 완료 | **처리 완료** — 라우트 전체를 try/catch로 감싸 JSON 오류로 답하고(속사정은 로그에만), 화면은 실패 시 **쓴 글을 되살리고** 낙관적 말풍선을 거둔다. 답변이 시작된 뒤의 실패는 되살리지 않는다. 커밋 `fa73470` |
| BL-021b | 2026-09-13 | **`maxDuration` 설정이 어디에도 없다** — 라우트·`vercel.json` 모두. 긴 요청이 플랫폼 기본 한도에 걸려 죽을 수 있다 | 복잡한 요청(회원가입+운영자화면+게시판)을 프로덕션에 보내니 **85초 넘게 사고(thinking)만 하고 글자 하나 안 나왔다**(실측). 화면에는 "고치는 중…"만 | **높음** | 처리 완료 | **처리 완료** — `export const maxDuration = 60`. 요금제 상한을 넘기면 배포가 거부되므로 보수적으로. 커밋 `fa73470` |
| BL-021c | 2026-09-13 | **`loadCurrentFiles`가 파일을 순차로 내려받는다** — 스트림이 열리기 **전에** 전부 끝나야 한다 | 빵집 프로젝트(글파일 11개)로 실측: **5.8초**(내려받기만 4.7초). Vercel 함수와 Supabase 리전이 다르면 더 나빠진다 | 보통 | 처리 완료 | **처리 완료** — `Promise.all`로 한꺼번에 내려받고 순서는 요청 순서로 되돌린다. 커밋 `fa73470` |
| BL-017 | 2026-09-13 | **검사가 한 번 거짓 통과했다.** `vitest run`이 디스크의 68개 중 **57개만 수집**하고 "Test Files 57 passed"로 보고 | 같은 명령을 다시 돌리면 68/661로 정상. 3회 연속 재현 안 됨. 원인 미상(윈도우에서 부하 중 디렉터리 열거가 일부만 돌아오는 것으로 추정) | 낮음 (거짓 **실패**가 아니라 거짓 **통과**라 위험하긴 하다) | 관찰 | 재현 안 됨. **대응: 검사 결과를 볼 때 '통과' 여부만 보지 말고 파일 수까지 대조한다.** BL-011과 같은 부류 — 게이트가 흔들리면 게이트가 아니다 |

## 처리 완료

| ID | 증상 / 요청 | 원인 | 처리 |
|---|---|---|---|
| BL-020 | (통합 점검 중 발견) **감사 기록 화면에서 자기 자신의 필터 조건이 `[object Object]`로 표시된다** | `/admin/audit`가 남기는 감사 기록 자신의 `detail.filter`가 중첩 객체인데, `audit-view.ts`의 모르는 detail 처리(`${k}=${v}`)가 중첩 객체를 문자열로 못 풀었다 | `formatValue`로 재귀 처리 — 중첩 객체도 "k=v" 쌍으로 펼친다. e2e로 실제 화면에 `[object Object]`가 없는 것 확인. 커밋 `39f70ae` |
| BL-015 | **첨부 파일이 영영 지워지지 않는다.** `attachments` 비공개 버킷이 계속 커진다 | `deleteConversationAttachments`는 [P7-8]부터 있었지만 대화 삭제 기능 자체가 없어 부를 자리도 없었다 — `dormant.test.ts`([P8-8])가 훑다가 찾아냄 | **프로젝트 삭제**가 지금 유일한 정리 동작이므로, 그 프로젝트를 만든 대화의 첨부까지 함께 지우도록 배선(파일·되돌리기 사본 정리와 같은 자리). 실패해도 삭제 자체는 끝낸다. e2e로 진짜 Storage에 파일을 올려 삭제 확인. **범위 밖으로 남김**: 발행(게시) 전(블록1~4)에 붙인 첨부는 프로젝트가 안 생겨 이 훅으로 안 지워진다 — 지금 규모에서는 느리게 쌓여 낮은 우선순위 유지. 커밋 `4744694` |
| BL-014 | **프로젝트 개수 한도가 집행되지 않았다.** 대시보드는 "프로젝트 0 / 1개"라고 알리는데 실제로는 무제한으로 만들 수 있었다 | `canCreateProject`는 [P6-3]부터 있었고 단위 테스트도 통과했지만 `src/` 전체에서 아무도 부르지 않았다 — `dormant.test.ts`([P8-8])가 훑다가 찾아냄 | **새 프로젝트를 만들려는 순간**(구현 단계 + 아직 연결된 프로젝트 없음)에만 막는다 — 계획·작업분해 단계나 있는 프로젝트를 고치는 turn까지 막지 않는다. `canStartChat`과 같은 이유로 Claude를 부르기 전에 확인. e2e로 진짜 DB에서: 한도 초과 시 Claude 미호출(응답 5초 미만)·402·메시지 미저장, 있는 프로젝트 고치기는 정상 진행. `dormant.test.ts`의 KNOWN_DORMANT에서 자동으로 빠짐. 커밋 `fd9fe04` |
| BL-019 | **통합 점검 중 실제 재현.** 되돌리기 직후 실제 파일은 옳았는데("오늘의 소금빵"), 이어서 사소한 걸 고치면 모델이 **되돌리기 전 이름("단팝빵 가게")을 되살려 저장**했다 | [BL-018]이 "지금 파일이 대화 기록보다 정확하다"는 안내를 시스템 프롬프트에 넣었지만, 여러 턴에 걸친 대화 자체가 그 한 줄 각주보다 모델에게 더 강하게 작용했다 | 되돌린 사실을 시스템 프롬프트가 아니라 **대화의 한 턴(assistant 메시지)**으로 남긴다 — 모델이 방금 자기 입으로 한 말은 몇 턴 전의 말보다 강하다. 실제 재현→수정→재확인까지 거침(같은 요청을 다시 보내 "오늘의 소금빵" 기준으로 정확히 반영되는 것을 실물로 확인). 커밋 `4200e29` |
| BL-018 | **유지보수 중인 모델이 지금 배포된 파일을 모른다.** "제목을 바꿔줘"라고 하면 "현재 내용을 붙여넣어 주시겠어요?"라고 되묻는다 | 유지보수 지시문은 "이미 만들어져 서비스되고 있다"고만 알려주고 **실제 파일은 주지 않았다**. 대화 기록에 남아 있으면 거기서 읽지만 ①기록이 길면 잘리고 ②**되돌리기 뒤에는 실제 파일과 어긋난다**(없는 버전을 고친다) | `loadCurrentFiles`로 지금 저장된 파일을 프롬프트에 싣는다. 예산(60KB·12개)을 넘으면 **자르지 않고 이름만** — 반쪽을 보여주면 모델이 반쪽으로 내고 나머지가 날아간다. 읽기 실패해도 던지지 않는다. e2e는 **대화 기록이 빈 채로** 고쳐달라고 해서 되묻지 않는 것까지 확인. 커밋 `8795227` |
| BL-016 | **가입자 2명(`lee.myungho@gmail.com`·`parkbc@kw.ac.kr`)이 서비스를 쓸 수 없었다.** 대화하면 402 "계정 정보를 확인할 수 없습니다"로 막히고, **운영 콘솔 목록에도 보이지 않았다**(목록이 `profiles`에서 온다) — 회원님이 알아챌 방법이 없었다 | `auth.users`에는 있는데 `profiles` 행이 없었다. 가입 트리거(`handle_new_user`)가 생기기 전에 가입한 계정으로 보인다. 트리거 자체는 지금 정상(실제로 만들어 확인) | 트리거가 그때 돌았다면 만들었을 값 **그대로** 복원 — 가입 시각 보존, 체험은 가입+7일. 둘 다 체험 5일 남은 정상 상태, 콘솔 목록에도 노출 확인. 복원 사실을 감사 기록에 남김(`correction: BL-016`). **되풀이 방지까지 완료**: 로그인할 때 프로필이 없으면 만든다(`ensureProfile`, `ensureAdminRole`보다 **먼저** — 그쪽은 update라 없는 행을 못 고친다). e2e에서 진짜 DB의 프로필을 지우고 로그인만으로 되살아나 402가 200이 되는 것까지 확인. 커밋 `ab90214` |
| BL-013 | (P8-5 작업 중 발견) **비상 차단(FR-016)이 동작하지 않는다** — `setProjectBlocked`를 부르는 곳이 없다 | [P8-0]에서 컬럼·함수를 만들고 배선을 안 했다. BL-008(`ensureAdminRole`)·`hasVerifiedMfa`와 같은 유형 — 슬라이스 9가 콘솔 안쪽만 검증해 드러나지 않았다 | [P8-5d] 접수함에서 신고 처리와 함께 가린다. 권한은 `artifact:block`으로 따로 판정. e2e에서 실제로 `/site/{주소}`가 404가 되고 **파일은 남아 있음**까지 확인. 커밋 `f546f28` |
| BL-012 | (P8-7 작업 중 테스트가 발견) **등급 부여 만료일이 고른 날짜보다 하루 뒤로 뜬다.** `2026-12-31`을 고르면 화면에 `2027. 1. 1.` | 고른 날짜를 **UTC 자정**(`T23:59:59Z`)으로 저장했다. 한국 시각으로는 다음 날 08:59:59 — 실제 만료가 의도보다 9시간 늦다. 살아있는 계정 1건이 그 상태 | [P8-7c] 한국 시각을 아는 자리를 `lib/time/seoul.ts` 하나로 모으고 `endOfDaySeoul` 사용. 관리 화면의 다른 날짜 표시도 함께 일원화. e2e로 저장값까지 고정. **지난 기록은 고쳐 쓰지 않는다** — 감사 화면에 잘못 저장된 값은 잘못된 그대로 보인다. 커밋 `f266364` |
| BL-011 | (BL-010 작업 중 발견) **단위 테스트가 3~8회에 한 번 거짓 실패**했다 — `tests/api/chat.test.ts` 2건 | 무거운 라우트를 **첫 테스트 본문 안에서** `await import` 해서 모듈 변환 비용(5.4초)이 첫 테스트에 청구됐고 5초 기본 제한을 넘겼다. 시간이 초과돼도 진행 중이던 호출은 계속 끝나 **다음 테스트의 mock을 오염**시켰다(2건 연쇄) | `beforeAll`에서 라우트를 한 번 데우고 `testTimeout` 20초. 5회 연속 561건 통과로 확인. 커밋 `e565050` |
| BL-010 | **크롬에서 `/admin`이 404**. Claude 브라우저에서는 정상 | 그 크롬 창이 관리자 아닌 계정(5개 중 4개는 `role=developer`)으로 로그인돼 있었다. [P8-11]은 그 경우 404를 주게 돼 있었으므로 **동작은 설계대로였다** — 그러나 설계가 틀렸다: 로그아웃하면 같은 주소가 로그인 화면을 내주므로 숨는 효과는 처음부터 없었고, 운영자만 막다른 길에 갇혔다 | [P8-12] 자격 없는 계정에게 지금 누구인지 알리고 계정 전환 버튼 제공. `adminLogoutAction`은 `/admin`으로 되돌린다. API의 404는 유지. 커밋 `6f41e9b` |
| BL-008 | **ADMIN_EMAIL로 가입했는데 계속 일반 개발자였다** | `ensureAdminRole`을 [P2-7]에서 만들고 **어디에서도 부르지 않았다**. 단위 테스트는 함수만 보므로 통과했다 — 함수가 있는 것과 동작하는 것은 다르다 | 로그인할 때마다 `promoteIfAdmin` 호출(가입 때만 하면 기존 계정은 영영 안 올라간다). `/api/health`에 `adminEmailConfigured` 추가. 커밋 `7228def`. 확인: `pinusian@gmail.com` → `role: admin` |
| BL-008b | (확인 중 혼선) 승격 뒤에도 대시보드에 **"체험 등급"**이 떠서 실패로 보였다 | 등급(`grade`)과 역할(`role`)은 별개다. 관리자도 자기 프로젝트에서는 체험 등급이 맞다(FR-018 역할 겸직). 화면에 역할이 전혀 안 나와서 구별할 길이 없었다 | BL-009로 이어짐 — 관리자에게만 보이는 "서버 관리" 입구가 곧 역할 표시가 된다 |
| BL-009 | **관리자로 승격돼도 들어갈 길이 없다.** `/admin` 링크가 코드 어디에도 없어 주소를 외워야 했고, `/admin`에 가면 개발자 로그인으로 **주소까지 바뀌었다** | 승격(FR-024)과 콘솔(FR-014)은 만들었지만 **둘을 잇는 입구**를 아무도 만들지 않았다. 슬라이스 9는 콘솔 안쪽만 검증했다 | [P8-11] `/admin`이 그 자리에서 관리자 로그인을 받고(FR-038), 화면을 반전 상단 띠로 구별하고(FR-039), 대시보드에 관리자 전용 입구(FR-040). e2e 6건으로 고정 |
| BL-006 | **슬라이스 9가 프로덕션에 배포되지 않았다** (`/admin`·`/api/admin/usage`가 404) | lint 경고를 없애려 `GET(_request)` → `GET()`으로 바꿨는데 테스트는 계속 인자를 넘겨 **타입 검사 실패**. 그 뒤 `npm run build`를 안 돌려 로컬에서는 안 보였고 Vercel 빌드만 깨졌다 | `rm -rf .next` 클린 빌드로 재현·수정. 커밋 `d72a209`. 배포 확인(401) |
| BL-007 | 한 저장소에 **Vercel 프로젝트가 3개** 붙어 push마다 빌드 3번·실패 메일 3통 | `sdvc-app-sx72`·`sdvc-app-b5vk`가 환경변수 없는 빈 사본이었다. [P0-2] 때 "보조 도메인"으로 잘못 알았던 것의 정체 | 사용자가 둘 삭제(2026-09-12). 확인: 두 호스트 404, `sdvc-app`만 200 |
| BL-001 | **"이어서 수정"에 들어가면 "대화가 끝났습니다"가 뜨고 프롬프트가 먹히지 않는다** | 구현을 마치면 대화 단계가 `done`이 되고 `/api/chat`이 모든 입력을 400으로 막았다. FR-025 진입점은 있는데 정작 쓸 수 없었다 | [P7-4b] 여섯 번째 블록 `maintenance` 신설. 이미 `done`인 대화는 읽을 때 유지보수로 변환(DB 미변경). 커밋 `62e7cf9`(RED)→`9e15684`(GREEN) |
| BL-001b | (BL-001 검증 중 발견) 모델이 "고쳤습니다"라고 답하는데 **홈페이지가 그대로였다** | 모델이 ```` ```html file:index.html ```` 처럼 **언어를 먼저** 적으면 파서가 못 읽고 파일을 조용히 버렸다 | [P7-4b] 언어 표시·공백 허용, 유지보수 지시에 형식 예시 추가. 같은 커밋 |
| BL-004 | 프롬프트에 **파일·이미지를 붙일 수 없다** | 업로드 경로 자체가 없었다 | [P7-8] 비공개 버킷 `attachments` + 앞머리 바이트 검사 · [P7-9] 서버가 id로 원본을 다시 읽어 image/text 블록으로 전달 · [P7-11] 입력창 📎. 커밋 `c343f90`·`49b60fb`·`27cbe4c` |
| BL-004b | (검증 중 발견) 코드는 유지보수로 넘기는데 **DB가 그 값을 거부**했다 | `conversations.current_block` CHECK 제약에 `maintenance`가 없었다. 단위 테스트는 목, e2e는 `done`만 써서 아무도 지나가지 않은 구간 | 마이그레이션 `0007`, 실제 DB에 넣어보는 e2e로 고정 |
| BL-005 | **자기 홈페이지에 자기 이미지를 넣을 방법이 없다** | 이미지는 모델이 글로 만들 수 없다 | [P7-10] ```use-image:경로@첨부id``` 지시 → 첨부 원본을 프로젝트 폴더로 복사. 커밋 `fe9ab0c` |
| BL-005b | (검증 중 발견) 사진은 저장됐는데 **화면에 아무 변화가 없었다** | 모델이 `use-image`만 내고 HTML을 고치지 않았다 | [P7-11] 새 사진인데 아무 파일도 그 경로를 쓰지 않으면 경고. 프롬프트에도 "이 지시만으로는 보이지 않는다" 명시 |
| BL-003 | 이용 상태 카드가 프로젝트 목록보다 **위**에 있어 눈에 먼저 들어온다 | 정보 위계가 뒤집혀 있었다 — 주인공은 목록이다 | [P7-1c] 목록 아래로 이동 + 세로 97→66px(68%)·글자 14→10px(71%), 가로 동일. 실제 렌더링 값을 재는 e2e 4건으로 고정. 커밋 `f489378` |
| BL-002 | 새 프로젝트에 **이름을 정할 수 없고** 나중에 바꿀 수도 없다 (전부 "내 프로젝트") | 이름 컬럼은 있었지만 넣고 고치는 길이 없었다 | [P7-1b] 시작할 때 이름 입력(비워도 됨, 비우면 첫 요청 문장으로 작명 — 모델 호출 없음) + 목록에서 이름 변경. 커밋 `fe4b439`(RED)→`15a7edb`(GREEN) |

## 남은 위험

- **로그인하지 않는 동안은 여전히 막혀 있다.** [BL-016] 자가 복구는 **로그인 시점**에
  돈다. 세션이 살아 있는 채로 프로필만 사라진 사람은 다시 로그인할 때까지 402다.
  지금 방식(`ensureAdminRole`을 배선한 자리와 같음)을 유지하되, 화면마다 쓰기를
  거는 것은 값이 비싸서 하지 않았다. 문제가 실제로 드러나면 그때 본다.
