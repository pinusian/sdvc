# tasks.md — MVP 작업 분해 (버티컬 슬라이스)

> 작성일: 2026-09-10 · [P1-4] Tasks · 게이트 G4 승인 대상
> 범위: WBS Phase 2~5 (MVP). Phase 6 이후는 해당 시점에 별도로 Tasks 블록 재진행.

## 슬라이스 개요

| 슬라이스 | WBS Phase | 작업 수 | 예상 세션 | 완결 기준 |
|---|---|---|---|---|
| 1. 로그인·인증·권한 | Phase 2 | 9개 | 8 | 가입→로그인→대시보드→로그아웃이 브라우저에서 실제로 됨 |
| 2. SDVC 엔진 | Phase 3 | 7개 | 8.5 | 채팅으로 헌장~계획까지 대화가 실제로 진행됨 |
| 3. 산출물 생성·저장 | Phase 4 | 5개 | 4 | 구현 단계 후 Storage에 파일이 실제로 쌓임 |
| 4. 산출물 URL 서빙 | Phase 5 | 5개 | 6 | 가입부터 URL 접속까지 전 과정이 실제로 동작함(E2E) |

각 슬라이스는 **기능 하나 = 완결된 조각**입니다. 슬라이스 1이 끝나기 전엔 슬라이스 2를 시작하지 않습니다 — 로그인 없이 채팅 기능만 먼저 만들면, 나중에 권한 체계를 끼워 넣기가 더 어렵습니다.

## 슬라이스 1 — 로그인·인증·권한 (Phase 2)

| ID | 작업 | TDD 단계 |
|---|---|---|
| P2-1 | Next.js 프로젝트 생성 | — (뼈대) |
| P2-2 | Vercel 첫 배포 | — (배포 확인) |
| P2-3 | Supabase 연결·환경변수 틀 | — (연결 확인) |
| P2-4 | 역할·등급·권한 DB 설계 | — (스키마) |
| P2-5 | 개발자 회원가입·로그인 | RED(가입 실패 테스트) → GREEN(구현) → REFACTOR |
| P2-6 | 권한 검사 공통 모듈 | RED → GREEN → REFACTOR |
| P2-7 | 서버관리자 계정·2FA (환경변수 `ADMIN_EMAIL` 지정 이메일 가입 시 자동 role='admin', FR-024) | RED → GREEN → REFACTOR |
| P2-8 | 로그인 화면·대시보드 | 화면 구현 + Playwright E2E |
| P2-9 | 슬라이스 1 검증 | 브라우저 실행 증거 수집 |

## 슬라이스 2 — SDVC 엔진 (Phase 3)

| ID | 작업 | TDD 단계 |
|---|---|---|
| P3-1 | ANTHROPIC_API_KEY 설정 | — (사용자 작업) |
| P3-2 | 대화 API 뼈대 | RED → GREEN → REFACTOR |
| P3-3 | SDVC 진행대본 이식 | RED(대본 준수 테스트) → GREEN → REFACTOR |
| P3-4 | 대화 상태 저장 | RED → GREEN → REFACTOR |
| P3-5 | 채팅 화면 | 화면 구현 + 스트리밍 확인 |
| P3-6 | 승인 게이트 UI | 화면 구현 |
| P3-7 | 슬라이스 2 검증 | 브라우저 실행 증거 수집 |

## 슬라이스 3 — 산출물 생성·저장 (Phase 4)

| ID | 작업 | TDD 단계 |
|---|---|---|
| P4-1 | Storage 버킷·권한 정책 | — (설정) |
| P4-2 | 프로젝트 메타 표 설계 | — (스키마) |
| P4-3 | 파일 생성·저장 로직 (**생성+삭제**, FR-022) | RED → GREEN → REFACTOR — 저장과 삭제(DELETE /api/projects/[id])를 같은 슬라이스에서 함께 구현 |
| P4-4 | 생성 진행 표시 UI | 화면 구현 |
| P4-5 | 슬라이스 3 검증 | Storage 확인 증거 |

## 슬라이스 4 — 산출물 URL 서빙 (Phase 5)

| ID | 작업 | TDD 단계 |
|---|---|---|
| P5-1 | 산출물 서빙 라우트 | RED → GREEN → REFACTOR |
| P5-2 | 파일형식·자산 처리 | RED → GREEN → REFACTOR |
| P5-3 | 공개범위 설정 | RED → GREEN → REFACTOR |
| P5-4 | 내 프로젝트 목록 화면 | 화면 구현 — **[P4-4]에서 선행 구현 완료** |
| P5-4b 🆕 | 이어서 수정·추가 요청 진입점 (FR-025) | RED → GREEN — 대시보드의 프로젝트에서 그 프로젝트를 만든 대화로 들어가기. 2026-09-11 사용자 요구로 추가. 엔진은 이미 같은 프로젝트에 덮어쓰므로([P4-3]) 진입점만 있으면 버그 수정·기능 추가가 바로 가능하다. 본체(버그 등록·추적 P7-2/3, 기능요청 흐름 P7-4, 롤백 P7-6)는 Phase 7 유지 |
| P5-5 | MVP 전 구간 E2E 검증 (★게이트 G5) | 가입→로그인→대화→생성→URL 접속 전체 실행 증거 |

## 슬라이스 5 — 유료화: 결제·등급 (Phase 6) — 2026-09-11 추가

MVP(슬라이스 1~4) 완료 후 착수. WBS P6-1~P6-8을 TDD 단위로 다시 쪼갠 것.
**돈이 오가는 구간이라 "서버가 판정한다"와 "위변조 검증"이 핵심이다.**

| ID | 작업 | TDD 단계 |
|---|---|---|
| P6-1 | Stripe 계정·상품 등록 (테스트 모드) | — (사용자 작업). 기본 $12 / 프로 $35, USD (FR-028) |
| P6-2 | 사용량 기록 `usage_logs` + 이번 달 집계 | RED → GREEN — 대화마다 토큰 사용량을 남기고 월 합계를 낸다 |
| P6-3 | 접근 판정 모듈 (체험·구독·한도) | RED → GREEN — "지금 이 요청을 허용하는가"를 **서버에서** 한 곳으로 판정 (FR-008·FR-026). 브라우저 판정은 우회 가능하므로 금지 |
| P6-4 | `/api/chat`에 판정 연결 + 차단 안내 | RED → GREEN — 한도 초과·체험 만료 시 402/403과 업그레이드 안내 |
| P6-5 | Stripe Checkout 연동 | RED → GREEN — 카드정보는 Stripe가 직접 받고 우리 서버는 저장하지 않는다 |
| P6-6 | Webhook 처리 + 등급 자동 전환 | RED → GREEN — **서명 위변조 검증 필수**. 결제 성공→등급 상승, 해지·미납→강등 |
| P6-7 | 체험 만료·해지 시 산출물 처리 | RED → GREEN — 즉시 비공개 + 유예(체험 10일 / 해지 30일) 후 삭제 (FR-023·FR-027) |
| P6-8 | 요금제·결제·구독관리 화면 | RED → GREEN — `/pricing` 3등급 비교·결제 시작, 대시보드 이용상태(체험 잔여·사용량·한도), Stripe 고객 포털로 해지·영수증 (FR-023·FR-028) |
| P6-9 | 결제 전 구간 검증 (★게이트 G6) | **통과(2026-09-12)** — 실제 계정 1개로 가입→체험→만료→결제→등급상승→해지→강등 한 바퀴. 실행 중 해지 웹훅의 잠금 순서 결함 1건 발견·수정 |

## 슬라이스 6 — 긴급 수정·다듬기 (2026-09-12 추가)

쓰다가 발견한 5건 중 **작고 급한 3건**. `docs/backlog.md` BL-001~003.
Phase 7 본체보다 **먼저** 한다 — BL-001은 이미 파는 기능(FR-025)이 동작하지 않는 것이기 때문이다.
WBS에 없던 작업이므로 기존 Phase 7 항목에 가지번호를 붙였다([P5-4b] 때와 같은 방식).

| ID | 작업 | TDD 단계 |
|---|---|---|
| P7-4b 🆕 | **유지보수 블록 신설** (FR-029, BL-001 **치명**) | RED → GREEN — 구현 후 `done` 대신 `maintenance`로. **"완료된 대화에 입력이 된다"는 실패 테스트부터**(SC-008 재현 우선). 이미 `done`인 과거 대화도 열리게 취급 |
| P7-1b 🆕 | 프로젝트 이름 지정·변경 (FR-030, BL-002) | RED → GREEN — 새 프로젝트에서 이름 입력(빈칸 허용, 비면 첫 메시지로 자동), `PATCH /api/projects/[id]`에 `name` 추가, 대시보드에서 수정 |
| P7-1c 🆕 | 이용 상태 카드 위치·크기 (FR-033, BL-003) | RED → GREEN — 프로젝트 목록 **아래로**, 글자·세로 70%(가로 동일) |
| P7-1d 🆕 | 슬라이스 6 검증 | 실제 화면에서 "이어서 수정 → 고쳐줘 → 반영" 한 바퀴 + 이름 변경 + 배치 확인 (증거 첨부) |

## 슬라이스 7 — 프롬프트 첨부와 이미지 (2026-09-12 추가)

`docs/backlog.md` BL-004·005. **새 기능**이라 범위가 크다 — 업로드 보안·토큰 비용이 함께 걸린다.

| ID | 작업 | TDD 단계 |
|---|---|---|
| P7-8 🆕 | 첨부 업로드·검사 (FR-031) | RED → GREEN — 비공개 버킷 `attachments`, `POST /api/attachments`. 형식은 **확장자가 아니라 파일 앞머리 바이트**로 판정, 파일당 10MB·최대 5개, 실행·압축 파일 거부 |
| P7-9 🆕 | 첨부를 Claude에 전달 + 사용량 (FR-031) | RED → GREEN — `/api/chat`이 `attachmentIds`를 받아 **서버가 Storage에서 다시 읽어** 이미지/글 블록으로 싣는다(클라이언트 내용 불신). 이미지 토큰도 같은 `usage_logs`·같은 월 한도 |
| P7-10 🆕 | 산출물에 이미지 넣기 (FR-032) | RED → GREEN — ```` ```use-image:경로@첨부id``` ```` 지시를 발행 단계가 읽어 원본을 프로젝트 폴더로 복사. 형식이 어긋나면 **조용히 무시하지 않고 화면에 알린다** |
| P7-11 🆕 | 슬라이스 7 검증 | 실제 이미지를 첨부해 홈페이지에 넣고 `/site/{주소}`에서 **눈으로 보이는 것**까지 확인 (증거 첨부) |

> 이 두 슬라이스가 끝난 뒤 **Phase 7 본체**(P7-2 버그 등록·추적, P7-3 버그→수정 연결, P7-5 신고 채널, P7-6 롤백, P7-7 검증)로 간다. 그 시점에 Block1~4를 다시 짧게 반복한다.

## 커밋 컨벤션 (실제 적용)

`{type}: [P#-#] {제목} - {단계}`
예: `test: [P2-5] 회원가입 - RED`, `feat: [P2-5] 회원가입 - GREEN`, `refactor: [P2-5] 회원가입 - REFACTOR`
